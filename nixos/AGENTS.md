# nixos

shared linux layers and profile composition live here.

- read the relevant profile before changing shared linux modules
- keep networking, features, home wiring, and profiles separated by concern
- do not move host-specific policy into shared modules without clear reuse
- verify against the affected flake output(s) only
